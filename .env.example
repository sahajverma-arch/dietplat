# Supabase project settings → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase project settings → Database → Connection string (use the pooler
# connection string for serverless/edge-friendly connections)
DATABASE_URL=

# NVIDIA NIM, OpenAI-compatible endpoint: https://integrate.api.nvidia.com/v1
NVIDIA_API_KEY=
# Model slug from the NVIDIA NIM catalogue, verified live against
# https://integrate.api.nvidia.com/v1/models in Prompt 7 (2026-08-08).
# meta/llama-3.3-70b-instruct over 3.1: same class/price, newer, better
# instruction-following for the strict-JSON food-selection task.
NVIDIA_MODEL=meta/llama-3.3-70b-instruct
